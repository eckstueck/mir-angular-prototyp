/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GenrePipe } from './genre.pipe';

describe('Pipe: Genree', () => {
  it('create an instance', () => {
    let pipe = new GenrePipe();
    expect(pipe).toBeTruthy();
  });
});