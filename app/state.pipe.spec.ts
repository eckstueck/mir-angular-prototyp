/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StatePipe } from './state.pipe';

describe('Pipe: Statee', () => {
  it('create an instance', () => {
    let pipe = new StatePipe();
    expect(pipe).toBeTruthy();
  });
});